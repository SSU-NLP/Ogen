[project]
name = "ogen-ui-monorepo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.26.0",
    "langchain>=1.2.4",
]

[tool.uv.workspace]
members = ["apps/server", "packages/ogen_stream"]
