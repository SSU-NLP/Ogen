# Shared Templates

Put cross-agent prompt, report, or config templates here when they are useful across more than one tool.

Keep tool-specific templates in the tool's native directory only when that tool requires a special format.
